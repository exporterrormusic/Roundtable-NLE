================================================================================
 FABLE_FIX.TXT — Remaining work
 Roundtable NLE — last updated 2026-06-10 by Claude (Fable 5)

 ALL code work from this plan is complete (Release build clean, 688/688 core
 tests green) — see git history.  Final batch: full .string() elimination
 (125 sites) + check_path_string.ps1 CI guard, diagnostic-launcher
 consolidation into launch.ps1 (launch.bat untouched — stays the fast pure-
 batch path), the 4.1 refactor (ConvertDecodedFrame.{h,cpp} — ONE CPU
 decode→BGRA core shared by urgent/prefetch/loop paths), and the 4.2 slice
 (VideoFrameMapping.h mapTickToSourceFrame — ONE tick→source-frame
 authority; fixed a real fps-priority mismatch where the pending-frame
 collector could prefetch a different frame than the renderer requested).
 Net session footprint: ~50 files, −380 lines.

 What remains is verification only:

────────────────────────────────────────────────────────────────────────────────
 VERIFICATION (needs a human at the keyboard)
────────────────────────────────────────────────────────────────────────────────

  1. Runtime smoke of the June-10 refactors in a normal editing session —
     the decode/convert and frame-mapping unification preserved each path's
     behavior by construction, but pixels on screen are the real test:
       - Scrub + play a project with Wells (packed-alpha), GREEN-keyed
         media, a looping character, and a still PNG: colors stable (no
         brightness/saturation flicker across cache churn), characters
         loop, stills don't vanish.
       - Play at 2x and on a speed-changed clip: motion stays smooth
         (no repeat/skip judder at clip starts).
       - Audio FX preview: EQ/Dynamics audible immediately and live while
         scrubbing a knob during playback; listen across a window swap on
         a LONG audio clip for boundary ticks.
       - The diagnostic launchers (launch_debug.bat, launch-framehash.bat,
         launch-spinediag.bat, launch_with_validation.bat) now route
         through launch.ps1 — run whichever you use next and confirm it
         behaves as before.

  2. PageHeap confirmation session for the (believed fixed) 0xC0000374:
     setup_appverif.ps1 → replay the old repro (cross-sequence paste +
     Wells packed-alpha scrub/settle + export) → disable.  A clean session
     closes the heap-corruption investigation for good.

  3. Trigger .github/workflows/asan.yml once via workflow_dispatch to
     validate it on the runner; promote to a PR gate after a clean streak.
     The new path::string() guard runs in the regular tests.yml on the
     next push — no action needed beyond watching it pass.

================================================================================
 END OF FILE
================================================================================
